import { CARD_LIST_TEXT } from 'constants/text';

export const FILTERING_ID = {
  all: 'all',
  favorites: 'favorites'
};

export const FILTERING_LABEL = {
  [FILTERING_ID.all]: CARD_LIST_TEXT.all,
  [FILTERING_ID.favorites]: CARD_LIST_TEXT.favorites
};
